/**
 * simulate stub (Block A1): the real harness arrives with Block B6.
 * Planned interface: pnpm simulate -- --runs 1000 --preset consensus --bot hedger
 * Emits endings distribution, turn counts, resource trajectories (CSV + summary).
 */
console.log('simulate: engine not built yet (arrives with Milestone B). No runs executed.');
